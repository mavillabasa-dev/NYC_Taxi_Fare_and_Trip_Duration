# Offline pipeline dependencies (src/ and notebooks/) — host only.
# The container installs api/requirements.txt instead; keep that one minimal.
# Ticket: T-101

# Data
pandas>=2.2
pyarrow>=16.0
numpy>=1.26

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# Notebooks
jupyter>=1.0
ipykernel>=6.29

# Modelling (T-106 baselines / T-109 selection)
scikit-learn>=1.5

# Geospatial — taxi zone shapefile, zone centroids, choropleth (T-116, T-103, T-111)
geopandas>=1.0

# Gradient boosting (T-107) — uncomment when starting the ticket
# lightgbm>=4.3
# xgboost>=2.1
